<header class="header container-fluid d-flex align-items-center">
    <nav class="container navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">KGB Corp.</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a {% if app.request.get('_route') == 'home' %} class="nav-link active" {% else %} class="nav-link" {% endif %} aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a {% if app.request.get('_route') == 'login' %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="/login">Login</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>